# Local-only configuration template. Do not commit real values.
# Empty values keep provider and persistence credentials out of this snapshot.
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat
DATABASE_URL=
# Default loopback endpoint for an explicitly started local backend.
FINANCIAL_API_URL=http://127.0.0.1:8000
